{
  "name": "@bringme/client",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:itch": "tsc --noEmit && vite build --base ./ --outDir dist-itch",
    "preview": "vite preview"
  },
  "dependencies": {
    "@bringme/shared": "*",
    "bitecs": "^0.3.40",
    "three": "^0.178.0"
  },
  "devDependencies": {
    "@types/three": "^0.178.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
